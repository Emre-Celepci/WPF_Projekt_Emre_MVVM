<Page x:Class="WPF_Projekt_Emre.pages.StartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      MinHeight="600"
      MinWidth="500"
      Title="StartPage">

    <Page.Background>
        <ImageBrush ImageSource="/assets/Images/Layout/Background.png"/>
    </Page.Background>

    <StackPanel HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="#AAFFFFFF"
                Margin="20">

        <TextBlock Text="Willkommen!"
                   FontSize="30"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <Button Content="Spiel starten"
                Command="{Binding StartGameCommand}"
                Margin="5"/>

        <Button Content="Highscore anzeigen"
                Command="{Binding ShowHighscoreCommand}"
                Margin="5"/>
    </StackPanel>
</Page>